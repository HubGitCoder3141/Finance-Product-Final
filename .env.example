# Informational example; the app reads operating-system environment variables.
COACH_DB_PATH=runtime/coach.sqlite3
# Future integration only. Neither variable enables a live provider in this build.
ANTHROPIC_API_KEY=dummy-not-a-real-key
COACH_PROVIDER=placeholder
